# `@level0x40/react-git`

Changesets manages this file. Add a changeset with `pnpm changeset` and the
release workflow rolls the entries into a versioned section here when it
opens the "Version Packages" PR.
