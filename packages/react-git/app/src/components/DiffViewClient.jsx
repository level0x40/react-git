/**
 * Skeleton — real implementation at
 * `app/src/impl/components/DiffViewClient.jsx`. See `BlobMediaClient.jsx`
 * skeleton for the `"use client"` boundary note. No default export;
 * only named exports (`DiffViewProvider`, `DiffBodyShell`,
 * `DiffViewToggle`).
 */
export * from "virtual:rg/component/DiffViewClient";
