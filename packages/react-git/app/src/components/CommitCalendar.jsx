/**
 * Skeleton — real implementation at
 * `app/src/impl/components/CommitCalendar.jsx`. The shipped pages
 * import from `./CommitCalendar.jsx`; this skeleton forwards through
 * `virtual:rg/component/CommitCalendar`, which the `template-plugin`
 * resolves to either the user's override (if a `--template <dir>`
 * provided one at `<template>/src/components/CommitCalendar.jsx`)
 * or the built-in impl. See `pages/page.jsx` for the full rationale
 * on why the indirection is structural rather than alias-based.
 *
 * Edit `app/src/impl/components/CommitCalendar.jsx`, not this file.
 */
export { default } from "virtual:rg/component/CommitCalendar";
export * from "virtual:rg/component/CommitCalendar";
