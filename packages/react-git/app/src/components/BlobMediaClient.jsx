/**
 * Skeleton — real implementation at
 * `app/src/impl/components/BlobMediaClient.jsx` (note: that file
 * carries the `"use client"` directive at its top, so the bundler
 * sees a client boundary at the impl path; this skeleton remains a
 * plain server module that re-exports client references).
 *
 * No default export on this module — only named exports
 * (`BlobMediaProvider`, `BlobViewToggle`, `BlobBodyShell`).
 */
export * from "virtual:rg/component/BlobMediaClient";
