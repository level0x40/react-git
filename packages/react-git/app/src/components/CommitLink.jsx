/**
 * Skeleton — real implementation at
 * `app/src/impl/components/CommitLink.jsx`. The shipped pages and
 * other components import from `./CommitLink.jsx`; this skeleton
 * forwards through `virtual:rg/component/CommitLink`, which the
 * `template-plugin` resolves to either the user's override (if
 * `--template <dir>` provided one at `<template>/src/components/
 * CommitLink.jsx`) or the built-in impl. See `pages/page.jsx` for
 * the full rationale on why the indirection is structural rather
 * than alias-based.
 *
 * Edit `app/src/impl/components/CommitLink.jsx`, not this file.
 */
export { default } from "virtual:rg/component/CommitLink";
export * from "virtual:rg/component/CommitLink";
