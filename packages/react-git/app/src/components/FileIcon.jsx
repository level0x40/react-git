/**
 * Skeleton — real implementation at
 * `app/src/impl/components/FileIcon.jsx`. `fileKindFor` and other
 * named exports flow through the `export *` line, so callers like
 * `BlobView` that import named exports from this module continue to
 * work transparently.
 */
export { default } from "virtual:rg/component/FileIcon";
export * from "virtual:rg/component/FileIcon";
