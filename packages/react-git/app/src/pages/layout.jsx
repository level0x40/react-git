/**
 * Skeleton — see `pages/page.jsx` for the rationale. Real
 * implementation: `app/src/impl/pages/layout.jsx`.
 */
export { default } from "virtual:rg/page/layout";
export * from "virtual:rg/page/layout";
